{
  "name": "imdb-movie-search",
  "version": "1.0.0",
  "description": "",
  "source": "index.html",
  "scripts": {
    "dev": "del-cli dist && parcel --no-source-maps",
    "build": "del-cli dist && parcel build --no-source-maps",
    "serve-build": "del-cli dist && parcel build --no-source-maps && serve dist",
    "deploy": "npm run build && vercel --prod"
  },
  "keywords": [],
  "author": "Scott Ivan Fulguirinas",
  "license": "GPL-3.0",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.11.0",
    "del-cli": "^5.1.0",
    "parcel": "^2.11.0",
    "sass": "^1.69.7",
    "typescript": "^5.3.3",
    "serve": "14.2.1"
  },
  "targets": {
    "default": {
      "distDir": "dist"
    }
  }
}
